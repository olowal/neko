--	MessageSystem.lua
--	A basic message system, send and/or receive messages
--	Messages are buffered, which means they are sent the next frame before updating other systems

MessageSystem = MessageSystem or class()

function MessageSystem:init()

end

function MessageSystem:send()

end

function MessageSystem:register()

end

function MessageSystem:unregister()

end

function MessageSystem:update()

end
